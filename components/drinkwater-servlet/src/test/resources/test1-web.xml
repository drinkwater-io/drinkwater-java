<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE web-propertyResolver
        PUBLIC "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
        "http://java.sun.com/dtd/web-app_2_3.dtd">

<web-propertyResolver>

    <!-- the test parameter is only to be used for unit testing -->
    <!-- you should not use this option for production usage -->
    <context-param>
        <param-name>test</param-name>
        <param-value>true</param-value>
    </context-param>

    <!-- you can configure any of the properties on CamelContext, eg setName will be configured as below -->
    <context-param>
        <param-name>applicationBuilder</param-name>
        <param-value>test.drinkwater.servlet.TestServiceBuilderConfig</param-value>
    </context-param>

    <!-- register Camel as a listener so we can bootstrap Camel when the web application starts -->
    <listener>
        <listener-class>drinkwater.servlet.DWServletContextListener</listener-class>
    </listener>

</web-propertyResolver>
        <!-- END SNIPPET: web -->